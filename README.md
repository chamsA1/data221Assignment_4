Q1. Dataset Exploration
Loaded the Breast Cancer dataset, examined feature/target shapes, and analyzed class distribution to understand whether the data is balanced and suitable for classification.

Q2. Decision Tree (Entropy)
Trained a decision tree using entropy to measure information gain and evaluated its performance using training and test accuracy.

Q3. Controlling Tree Complexity
Applied constraints (e.g., max depth) to reduce overfitting and analyzed feature importance to improve model interpretability.

Q4.  Neural Network
Built and trained a neural network on standardized data, demonstrating how scaling improves optimization and model performance.

Q5. Model Comparison
Compared the decision tree and neural network using confusion matrices to evaluate differences in prediction behavior and performance.

Q6. CNN on Image Data
Trained a convolutional neural network on the Fashion MNIST dataset to classify images by learning spatial features.

Q7. Error Analysis
Analyzed CNN misclassifications using a confusion matrix and visual examples to identify weaknesses and suggest improvements.
